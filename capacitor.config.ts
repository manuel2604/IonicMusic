import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'platzi.manuelMusic.com',
  appName: 'PlatziMusic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
